import Vue from 'vue'

Vue.directive('smooth-load', {
  bind(el, binding, vnode) {
    // el.style.opacity = '0';
    // el.style.transition = 'opacity 100ms ease-in';
  },
  update(newV, oldV) {

  }
})
